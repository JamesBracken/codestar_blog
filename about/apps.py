from django.apps import AppConfig


class AboutConfig(AppConfig):
    """
    Creates the configuration for the about app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'about'
